sum(S, X, Y):-
    S is X + Y.

minus(M, X, Y):-
    M is X - Y.

prod(P, X, Y):-
    P is X * Y.

div(D, X, Y):-
    D is X / Y.

mod(MOD, X, Y):-
    MOD is X mod Y.

intdiv(I, X, Y):-
    I is X // Y.

power(P, X, Y):-
    P is X ^ Y.